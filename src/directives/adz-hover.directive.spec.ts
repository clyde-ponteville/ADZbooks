import { AdzHoverDirective } from './adz-hover.directive';

describe('AdzHoverDirective', () => {
  it('should create an instance', () => {
    // const directive = new AdzHoverDirective();
    // expect(directive).toBeTruthy();
  });
});
